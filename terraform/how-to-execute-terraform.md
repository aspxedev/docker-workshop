# How to create the Terraform project

First, we need to start the Terraform project. Let's execute the next command in the terminal:
```bash
terraform init
```

# Provision resources with Terraform
```bash
terraform apply
```

# Check the docker container is running
```bash
docker ps
```

# Destroy resourcers
```bash
terraform destroy
```